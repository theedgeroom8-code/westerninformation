# Odds API fixtures

Real responses from The Odds API captured 2026-09-26 (NFL full-game odds, one NFL game's quarter/half markets with and without Pinnacle, and one college game's quarter/half markets). Used by `scripts/board-selftest.js`, which shifts every kickoff time and `last_update` to *now* at load time so the tests never go stale.
